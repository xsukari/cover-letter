import * as data from "./data"
import { sonoLight, sofiaSansSemiCondensed } from "./fonts"

//const _address = (data.getData(data.categories.address) as data.Address)

export default function address() {
    return (
        <div className="h-[23.5%]">

        </div>
    )
}